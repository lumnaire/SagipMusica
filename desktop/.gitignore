# electron-vite output
out/

# electron-builder installers
release/

node_modules/

# Generated by `npm run seed` from the Supabase migrations; regenerate rather
# than hand-edit. Tracked deliberately, so a clone can build an installer
# without a database — see scripts/build-seed.mjs.
